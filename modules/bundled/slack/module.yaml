name: slack
version: "1.0.0"
type: notifier
description: "Slack webhook notifications for task completion and failure events"
author: "CLU"
dependencies: []

config:
  webhook_url:
    type: string
    required: true
    secret: true
    description: "Slack incoming webhook URL"
